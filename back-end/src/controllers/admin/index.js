const sales = require('./sales.controllers');
const details = require('./details.controllers');
const editSale = require('./editSale.controllers');

module.exports = {
  sales,
  details,
  editSale,
};
